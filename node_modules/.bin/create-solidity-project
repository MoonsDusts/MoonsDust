#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../create-solidity-project/setup.js" "$@"
  ret=$?
else 
  node  "$basedir/../create-solidity-project/setup.js" "$@"
  ret=$?
fi
exit $ret
